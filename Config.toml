build_mode = "release"
target_adapters = ["circom"]
target_platforms = [
    "android",
    "ios",
]
ios = [
    "aarch64-apple-ios-sim",
    "aarch64-apple-ios",
]
android = ["aarch64-linux-android"]
